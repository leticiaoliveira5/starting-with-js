const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // estrutura if else

// condicao ? se for verdadeira : se for falsa

console.log(resultado(7.1)) // Aprovado
console.log(resultado(6.8)) // Reprovado